version: 1.3.9

interface:
  customWelcome: 'Sail away'

endpoints:
  custom:
    - name: "KG-RAG"
      baseURL: "http://rag-server:8080/api/v1"
      models:
        default: ["rag-knowledge-graph"]
        fetch: true

    - name: "Ollama"
      apiKey: "ollama"
      baseURL: "http://ollama:11434/v1/"
      titleConvo: true
      titleModel: current_model
      models:
        default: ["llama3"]
        fetch: false
